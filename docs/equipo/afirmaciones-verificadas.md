# Afirmaciones del orquestador, pasadas por la Sesión 0

La regla: **todo lo que el orquestador escriba como HECHO sobre el código pasa por la Sesión 0
antes, o se escribe como PREGUNTA.**

Este fichero es el mecanismo, no el buen propósito. Sin él la regla es una intención; con él, cada
afirmación deja rastro y la siguiente se puede comprobar con un comando que ya está escrito.

## Cómo se usa

1. El orquestador va a escribir algo como hecho sobre el código → lo manda a la Sesión 0.
2. La Sesión 0 lo mide y añade una fila: **lo que se afirmó · lo que se midió · el comando exacto**.
3. Si no hay comando que lo mida, no es un hecho: es una pregunta, y se escribe como pregunta.

⚠️ La tercera columna es la que hace que esto valga. Una fila sin comando reproducible es una
opinión con dos columnas de adorno. Y el comando se escribe **entero y pegable**, no descrito.

⚠️ Y los números caducan. La columna del medio dice lo que se midió **el día que se midió**; el
comando es lo que permite volver a preguntarlo hoy. Cuando los dos discrepan, no es que la fila
esté mal: es que el árbol se movió, y ése es justo el dato.

## Las nueve que ya conocemos

Sembradas el 8-sep-2026 sobre `origin/main` = `da5ac06a`. Las marcadas **[re-medido hoy]** se han
vuelto a correr al escribir esta tabla; las marcadas **[del registro]** se conservan con su comando
pero **no** se han vuelto a medir — y eso se dice en vez de callarlo.

| lo que se afirmó | lo que se midió | comando exacto |
| --- | --- | --- |
| «171 llamadas a git» | **71**. Se contaba TEXTO, no llamadas. **[re-medido hoy: 201]** — el número está vivo y crece; lo que no cambia es que hay que contar llamadas, no menciones | `node --input-type=module -e "const {censarReferenciaMovil}=await import('./tests/_censo-referencia-movil.mjs'); const c=censarReferenciaMovil(process.cwd()); console.log(c.llamadas)"` |
| «cuatro sitios del atajo N» | **seis**. **[re-medido hoy: 6]** | `node scripts/verificacion-s5/kbd-en-botones-de-crear.mjs` |
| «el NIF se imprime en el PDF» | **no se imprime**. **[del registro]** — hoy `pdf.service.ts` nombra `taxId` 8 veces, pero son declaraciones de tipo y el flag `docFields.taxId`: **nombrarlo no es imprimirlo**, y eso se decide leyendo el texto del PDF generado, no el fuente | `node --test tests/scrum604-desglose-en-el-pdf.test.mjs` (usa `tests/_texto-del-pdf.mjs`, que lee el TEXTO real del PDF y no su tamaño en bytes) |
| «SCRUM-724: hay que decidir los 44 px» | **el ticket no existía**: ya estaba decidido en SCRUM-352. **[del registro]** | `git log --oneline --grep="SCRUM-724" origin/main` y `ls tests/ \| grep 352` |
| «SCRUM-822: main está rojo» | **main estaba verde**: 5909 tests, 0 fail, `exit 0` — más 48/48 del banco `LIBRO_PG_URL` que CI sí corre. El 404 que se vio era real, pero **de un worktree con un punto en la ruta** (`.claude/worktrees/…`), no de la landing | `git checkout --detach origin/main && npm run build && node --test --test-force-exit tests/*.test.mjs` |
| «la tabla ocupa 730 px» | **978**. **[del registro]** — una caja CSS no es lo que ocupa: hay que medir el DOM ejecutado | `node scripts/censo-objetivo-tactil-panel.mjs` (mide sobre el navegador, no sobre la hoja de estilos) |
| «faltan los objetivos táctiles» | **ya estaban hechos** (SCRUM-720d). **[del registro]** — hoy el guard declara 15 excepciones con su motivo, que es distinto de «faltan» | `grep -c "sel: 'BUTTON" scripts/guard-objetivo-tactil.mjs` y `npm run guard:objetivo-tactil` |
| «el auto-merge falla por el conflicto» | **fallaba por permisos**. **[del registro]** — no se puede medir desde aquí: hace falta el log del workflow | `gh run list --workflow=pr-automatico.yml` — ⚠️ **`gh` NO está instalado en este entorno**, así que esta fila queda declarada como NO verificable localmente |
| «Jira no funciona en Visual» | **sí funciona**. **[re-medido hoy]** — el MCP de Atlassian ha contestado en esta misma sesión sobre `cloudId 30938fdf-…` | consulta JQL por el MCP de Atlassian; sin MCP, `docs/equipo/00-normas-comunes.md` no promete otra vía |

## Casos nuevos

Primer caso real del filtro: **dos sesiones escribieron dos hechos incompatibles y hubo que medir
cuál era.** Y el tercero es mío, que es lo que hace que esto valga para todos.

| lo que se afirmó | lo que se midió | comando exacto |
| --- | --- | --- |
| **S0:** «`revert-1192-…` ya no existe en el remoto, sólo queda su ref local; en un clon nuevo no pasaría» · **S3:** «main está rojo por esa rama REMOTA» | **Gana S0.** La rama NO está en el remoto: `ls-remote` devuelve vacío (control: ve 103 refs). Era una ref local obsoleta, y tras `git fetch --prune` **SCRUM-753 pasa a verde**. ⚠️ Declarado: el `for-each-ref` sale vacío hoy porque yo mismo pruné en el turno anterior — la prueba es `ls-remote`, que ya daba 0 ayer con la ref local presente | `git ls-remote --heads origin \| grep revert-1192` · `git for-each-ref refs/remotes/origin/ \| grep revert-1192` · `git fetch --prune origin && node --test tests/scrum753-censo-de-alcanzabilidad.test.mjs` |
| «main está verde en CI» (mi primera lectura del listado de Actions) | **ROJO.** El listado se leyó mal; la página del run dice `Failure` y «build + tests (con banco desechable)» sale con `exit code 1`. Run #2140 sobre `da5ac06a`. 🔒 Gana la fuente detallada sobre el resumen | `https://github.com/lwislg99/cobroflash-backend/actions/runs/34210697350` — ⚠️ los LOGS no son visibles sin `gh`, así que la causa no se lee de CI: se sostiene reproduciéndola |
| «el tapón de los 22 PR son 22 problemas» | **Es UNO, y es mío.** Sobre el mismo `da5ac06a`, en local y con las refs ya podadas, falla **sólo `scrum804-la-rama-viva`** (3 de 8), por la barrida de 456 ramas que ejecuté: su control enumerado busca la rama de SCRUM-821 —que borré, y con razón: su trabajo está en main por el PR #1167— y sus dos suelos codifican «558 ramas» y «464 dentro de main» | `git checkout --detach origin/main && node --test tests/scrum804-la-rama-viva.test.mjs` |
| **El fundador**, en el prompt de arranque del orquestador (9-sep-2026): «lee `YAQU_MASTER.md`», situándolo en la RAÍZ del repositorio | **No está en la raíz, y no ha estado nunca.** Vive en `docs/YAQU_MASTER.md`, 1.882 líneas. No es que se moviera: sobre la ruta de raíz, `git log --all` no devuelve **ni un commit** en ninguna rama. Lo reconoce él mismo — *«lo escribí mal en tu prompt, sin leerlo»* — y queda como **error nº 16**. 🔒 Es el primero que no es del asesor, y por eso importa: **la regla no mira quién escribe. Una ruta citada de memoria es una afirmación sobre el código**, la escriba quien la escriba | `for f in YAQU_MASTER.md docs/YAQU_MASTER.md; do test -f "$f" && echo "OK $f" \|\| echo "FALTA $f"; done` → `FALTA YAQU_MASTER.md` / `OK docs/YAQU_MASTER.md`. **Con su control positivo**, porque un vacío solo no distingue «no existe» de «el comando no mira donde debe»: `git log --all --oneline -- 'YAQU_MASTER.md' \| wc -l` da **0** frente a `git log --all --oneline -- 'docs/YAQU_MASTER.md' \| wc -l`, que da **273** (medido 9-sep-2026 sobre `origin/main` = `af3c50d8`, worktree `wt-jefe`) |

| **Yo mismo**, 9-sep-2026, en el mismo mensaje: «acabo de contar **1.318 ramas remotas**» (para sostener que la población del guard de SCRUM-833 crece sola) y, tres párrafos después, «me salieron **139 y luego 135**, así que ese número no te lo doy como bueno» | **108 ramas remotas vivas.** Ninguno de mis dos números contaba ramas: `git branch -r` mezcla tres poblaciones distintas y en este árbol da 1.322 = **110** refs de `origin` + **1.212** bajo `refs/remotes/pr/*`, que son **cabezas de pull request, no ramas** (heredadas por refs compartidos: el refspec de `origin` es solo `refs/heads/*`, así que las puso otra sesión). Además tenía **2 refs rancias** que el servidor ya no sirve. 🔒 **El error no es el número: es que cuestioné el que usé como DATO y no el que usé como ARGUMENTO.** Un número que sostiene una conclusión merece más escrutinio que uno que solo se reporta, no menos. Coincide con la Sesión 0 (~98-104) en cuanto se pregunta al servidor en vez de a mis refs | **Al servidor, que es la única fuente autoritativa:** `git ls-remote --heads origin \| wc -l` → **108**. Las tres poblaciones, separadas y nombradas: `git for-each-ref refs/remotes/origin/ \| wc -l` (110, mis refs, incluye rancias) · `git for-each-ref refs/remotes/pr/ \| wc -l` (1.212, cabezas de PR) · `git for-each-ref refs/heads/ \| wc -l` (613, locales). Control de refs rancias: `comm -13 <(git ls-remote --heads origin \| sed 's\|.*refs/heads/\|\|' \| sort) <(git for-each-ref --format='%(refname:short)' refs/remotes/origin/ \| sed 's\|^origin/\|\|' \| sort)` |
| **El traspaso del 9-sep** (y yo repitiéndolo sin medirlo): «Queda SCRUM-833 (`scrum637:164`) **a 4 ramas de caer**, con la misma forma» | **Ya está desactivado y en `main`.** El commit `4911488d` («scrum637 deja de medir el envase») es ancestro de `origin/main`. El umbral vivía en la línea **104**, no en la 164, y hoy ahí solo queda su lápida: *«AQUÍ HABÍA `inst.ramas.length > 50`»*, sustituido por `merges.length > 0`, que no caduca. El propio comentario da la cifra real del margen que hubo — **«con 97 refs vivas quedaban 47 de margen»**, no 4. El ticket sigue **En curso** en Jira con su rama dentro de main: es el caso espejo de la regla 42 | `git merge-base --is-ancestor 4911488d origin/main && echo MERGEADO` · `sed -n '104,108p' tests/scrum637-la-rama-que-nadie-mira.test.mjs` · barrido de la familia entera: `grep -rnE "ramas(\.length)? ?[<>]=? ?[0-9]+" tests/*.test.mjs` (medido 9-sep-2026 sobre `af3c50d8`) |
| **Yo mismo**, 15-sep-2026, al fundador: «los dos commits del 837 quedaron huérfanos: se empujaron cuatro minutos después del merge, el workflow terminó en verde y no se abrió ningún PR» | **Falso. Sí se abrió PR, y entró solo.** `pr-automatico` abrió el **#1249** y le armó el auto-merge (su log: «Auto-merge ARMADO en el PR #1249», veredicto `PR-ABIERTO`), y GitHub lo mergeó a las 09:28:37Z, cuatro minutos después del push. Mi medición fue `gh pr list --state open --head <rama>` → vacío, y leí «no hay PR ABIERTO» como «no se abrió PR». 🔒 **Una lista de abiertos vacía no distingue «nunca se abrió» de «se abrió y ya entró».** Y el caso real es el contrario de lo que dije: un push a una rama cuyo PR acaba de mergearse abre otro PR y entra solo — el bucle lo cubre | `gh run view 34952396521 --log | grep -E "Auto-merge ARMADO|PR-ABIERTO"` · `gh pr view 1249 --json state,mergedAt` (medido 15-sep-2026 11:39 CEST sobre `origin/main` = `51a28288`) |
| **La Sesión 2**, y yo repitiéndolo sin medir en su prompt del 9-sep: «`guard:lista-trabajos` es un TAPÓN COMÚN: cualquier PR que no toque `jobsView.js` sale rojo» | **Rojo común, sí. Tapón, no.** En `main` el ÚNICO check obligatorio es `build + tests (con banco desechable)`; «guards de navegador» no lo es. Pinta de rojo todos los PR pero **no impide que ninguno entre**: el #1248 se mergeó a las 09:29:16Z con ese job en `FAILURE`. Sigue valiendo arreglarlo —un check que no puede estar verde entrena a ignorar la suite— pero no para el bucle, y eso le cambia la prioridad | `gh api repos/lwislg99/cobroflash-backend/rules/branches/main --jq '.[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context'` · `gh pr view 1248 --json state,mergedAt,statusCheckRollup` |
| **Yo mismo**, 15-sep-2026 a las 11:42, en SCRUM-843: «al menos en esa pasada, el ancla nueva no le quitó el ciego» (leyendo el CIEGO del CI del PR #1250) | **Era otro ciego, con otra causa.** En `main` el arreglo funciona: los runs `34953539600` (contiene `8f67b88c`) y `34954547206` dan `guard:lista-trabajos` en verde. El CIEGO del PR lo midió la Sesión 2 y lo he comprobado: en un `pull_request` el job clona sin historia, `origin/main` no existe y el guard no resuelve el `merge-base` («no pude resolver el punto de partida de la rama»). Es SCRUM-851 y le pasa en todos los PR desde que el guard existe. Leí la línea del veredicto («NO MEDIDO (salida 2) · CIEGO en 1 guard(s)») y no la de la causa. 🔒 **Un mismo veredicto puede tener dos causas: se lee la línea que da la causa, no la que da el color.** | `gh run view --job <id> --log | grep -E "guard:lista-trabajos|NO SUPE MIRAR"` sobre el run del PR `34952731138` y el de main `34954547206` · `git merge-base --is-ancestor 8f67b88c $(gh run view 34953539600 --json headSha -q .headSha)` (medido 15-sep-2026 12:05 CEST sobre `origin/main` = `74477a45`) |
| **Yo mismo**, antes del 16-sep-2026: «lo caro en tokens es tragarse la salida de la tanda» | **Al revés.** La Sesión 0 midió **1.146 turnos** de 7 conversaciones (deduplicados por `message.id`: 20.225 repeticiones fuera): la caché LEÍDA es el **94,7 %** de 599,68M; lo escrito, el 0,5 %. El **62,6 %** del arrastre sale de turnos con el contexto por encima de 600k (52,9 % entre 600k y 900k), y **39 turnos de arranque o reanudación —el 3,4 %— escriben el 80,7 %** de la caché creada, ~598k cada uno. 🔒 **El coste no es lo que entra: es lo que se arrastra.** De aquí sale **A19** de `00-normas-comunes.md`. ⚠️ *SUPERADO el 17-sep-2026, por decisión del fundador tras la prueba de relevo de SCRUM-899: la lectura que A19 sacó de esta medición («mismo chat; chat nuevo solo si >1 h parado») la sustituye el relevo de sesión, que es la cuarta versión de A19. La medición no cambia.* ⚠️ **Corrección de la Sesión 0 sobre su propia cifra:** el primer informe decía 1.141 turnos, 19.886 repeticiones y 52,6 %, de una pasada anterior sobre una ventana móvil («los últimos 3 días» contados desde que se corría); con la ventana fija salen los números de arriba. Y «3 días» era la ventana pedida: los turnos que caen dentro van del 15-sep 09:15Z al 16-sep 12:49Z, **28 horas**. El «~7k por tanda frente a ~500k de arrastre» salió de un segundo script que no se ha comiteado, así que aquí no se afirma | `node scripts/medir-tokens.mjs --desde 2026-09-13T12:49:04Z --hasta 2026-09-16T12:49:05Z` — lee `~/.claude/projects/*/*.jsonl` del equipo donde se corre (en otra máquina da otra población, y lo declara en su primera línea). Sin `--hasta` la cifra se mueve con cada turno. El «39» depende de un umbral declarado en el script (escribir >200k de caché en un turno): con «crea más de lo que lee» son 46 y 82,7 % (medido 16-sep-2026 15:38 CEST sobre `origin/main` = `4b0d5739`) |
| **El orquestador**, 17-sep-2026 hacia las 08:30Z: «el checkout compartido `cobroflash-backend` va **3.782 commits** por detrás de `origin/main`» | **Cierto en su momento, y crece.** El checkout está en la rama `main` con `HEAD` = `5749f2f159a94c7fa6c91c6ce38524800ec52f64` (31-jul-2026), **0** commits por delante. Contra `aa465cdd`, que fue `main` de 08:24:25Z a 08:35:37Z, da **3.782** exactos; contra el `main` que servía el servidor a las 08:50:29Z (`1e7d6de2`), **3.791**. **Control positivo:** la diferencia, 9, es la que da `git rev-list --count aa465cdd..1e7d6de2` → **9**; y `1e7d6de2..1e7d6de2` → **0**. 🔒 Se cuenta contra el sha que da el SERVIDOR, no contra el `origin/main` local: las refs de los worktrees son compartidas y pueden estar rancias | En Git Bash: `S=$(git ls-remote origin refs/heads/main \| cut -f1); git fetch origin -q; C=d:/MILLONARIO/cobroFlash/cobroflash-backend; echo "detrás $(git -C $C rev-list --count HEAD..$S) · delante $(git -C $C rev-list --count $S..HEAD) · HEAD $(git -C $C rev-parse HEAD)"` (medido 17-sep-2026 08:50Z, hora de GitHub, sobre `origin/main` = `1e7d6de20a94bc67cad1dd8b04f00acfb8ec1507`) |
| **El orquestador**, 17-sep-2026: «los chats que se abren en `cobroflash-backend` cargan el `CLAUDE.md` del checkout (102 líneas, sin `docs/equipo`) y no el de `main` (160 líneas, con la LECTURA OBLIGATORIA de `00-normas-comunes.md`)» | **Los ficheros, medidos: cierto.** En el disco del checkout, `CLAUDE.md` tiene **102** líneas, **0** menciones de `docs/equipo` y **0** de «LECTURA OBLIGATORIA», y es idéntico a su `HEAD`; no hay `.claude/CLAUDE.md` ni `CLAUDE.local.md`. En `main` (`1e7d6de2`): **160** líneas, **2** de `docs/equipo`, **1** de `00-normas-comunes`, **2** de «LECTURA OBLIGATORIA». **Lo que CARGA Claude Code no lo mide un comando, así que eso queda como PREGUNTA:** «¿un chat abierto en `cobroflash-backend` recibe el `CLAUDE.md` del disco de esa carpeta, y no el de `main`?». Lo único observado es **1 sesión de 1**: la Sesión 0, abierta en el checkout el 17-sep, recibió al arrancar el contenido de `d:/MILLONARIO/cobroFlash/cobroflash-backend/CLAUDE.md`, sin `docs/equipo` ni LECTURA OBLIGATORIA; y trabajando después dentro de `.claude/worktrees/…` no le apareció en el contexto el de `main`. Coincide con la afirmación, pero una sesión no es la regla | Los ficheros, en Git Bash y tras `git fetch origin`: `C=d:/MILLONARIO/cobroFlash/cobroflash-backend; git -C $C show origin/main:CLAUDE.md > "$C/../claude-md-de-main.md"; for f in "$C/CLAUDE.md" "$C/../claude-md-de-main.md"; do echo "$f: $(wc -l < "$f") líneas · docs/equipo $(grep -c docs/equipo "$f") · LECTURA OBLIGATORIA $(grep -ci 'LECTURA OBLIGATORIA' "$f")"; done; git -C $C diff --quiet HEAD -- CLAUDE.md && echo "disco = HEAD"; rm "$C/../claude-md-de-main.md"`. Lo que carga: **no hay comando**; se contesta abriendo un chat en la carpeta y mirando qué `CLAUDE.md` le llega (medido 17-sep-2026 08:50Z sobre `origin/main` = `1e7d6de2`) |

| **El ticket SCRUM-989** (fundador, 21-sep-2026): «Un mapa (como el de Trabajos de hoy, pero en vivo)», es decir, que ya existe un mapa en «Trabajos de hoy» | **NO ENCUENTRO ese mapa** (y es un estado distinto de «no existe»). Sobre `origin/main` = `bbe01631ac5fcfd3ec67272c8aed3e39c192057f`, población **420 ficheros** de `public/` + `src/`: **0** «Trabajos de hoy», **0** `geolocation`, **0** proveedores de mapas (leaflet, mapbox, maplibre, google.maps, maps.googleapis, openstreetmap) en `public`, `src` y `package.json`, **0** `lat`/`lng`/`latitude`/`longitude` (palabra entera) en `prisma/schema.prisma`. **Suelo:** la misma búsqueda SÍ encuentra el único enlace a Google Maps, `public/dashboard/js/jobRailBlocks.js:86`, así que no está ciega. Ramas: de **147** remotas ninguna lleva mapa, geoloc, hoy, fichaje, ubicación ni dispatch en el nombre (control: la misma búsqueda ve `scrum-906k-familia-espanola`). 🔒 Falta lo que no se mide con un comando: si el fundador vio ese mapa en una pantalla que no está en `main` | `git ls-files public src \| wc -l` (420) · `git grep -n -i -w -E "lat\|lng\|latitude\|longitude" -- prisma/schema.prisma` (sale 1 = 0 hallazgos) · `git grep -n -i "geolocation" -- public src` (1) · `git grep -n -i -E "leaflet\|mapbox\|maplibre\|google\.maps\|maps\.googleapis\|openstreetmap" -- public src package.json` (1) · `git grep -n -i "trabajos de hoy" -- public src` (1) · CONTROL `git grep -n -i "google.com/maps" -- public src` (0, una línea) · `git ls-remote --heads origin \| wc -l` (147) (re-medido el 21-sep-2026 a las 12:28:20 GMT, hora de GitHub, sobre `bbe01631`) |
| **El ticket SCRUM-913**: «con empleados, el registro diario de jornada es obligación legal» · y, al prepararlo, la duda de si ya hay un Real Decreto de registro **digital** publicado | **Lo primero, cierto:** ET art. 34.9 (texto consolidado, «Última actualización publicada el 04/12/2025»): «La empresa garantizará el registro diario de jornada…», cuatro años, infracción grave (LISOS 7.5, multa 751-7.500 €). **Lo segundo, medido: NO está publicado.** Barrido de los sumarios del BOE del 30-sep-2025 al 21-sep-2026: **308** sumarios leídos (49 días sin BOE, 0 errores), **71.145** disposiciones y anuncios, 43 con «jornada» en el título, **2** con «registro de jornada / registro horario / control horario» y ninguno es esa norma (un perfil de consumo eléctrico y un contrato del Museo del Prado). Controles: encuentra la Resolución de 14-abr-2026 sobre jornada y horarios de la AGE (`BOE-A-2026-8287`), y el patrón dispara sobre dos títulos reales. **Límite declarado:** solo mira títulos. Lo que pase con el proyecto NO se mide desde aquí | `node docs/verificacion/barrido-boe-sumarios.mjs 2025-09-30 2026-09-21 salida.json` (resultado guardado en `docs/verificacion/barrido-boe-sumarios-2025-09-30_2026-09-21.json`) · el art. 34.9 vigente, en https://www.boe.es/buscar/act.php?id=BOE-A-2015-11430 (medido 21-sep-2026, informe completo en `docs/legal/REGISTRO_JORNADA_ES.md`) |

| **El orquestador**, 25-sep-2026 ~16:10Z: «producción se ha descongelado: el fundador aplicó los 2 ALTER y `yaqu.app` pasó de `b77a3cb3` a `cab692d5`» | **Cierto, medido directo:** `GET https://yaqu.app/version` (200) devuelve `{"version":"cab692d54fd183fb737d00365ee5dcdd3d6ea257"}`. Es SHA completo, no prefijo. Aprovechado para censar 6 funcionalidades de la semana (etiquetas/NIF/direccion SCRUM-1033/1034, contacto un toque SCRUM-1032, fotos en historial SCRUM-1061, chip WhatsApp en la lista SCRUM-986): las 4 tienen su marcador de codigo presente en el JS servido HOY por `yaqu.app` (`c360-chips`, `contacto-fila`, `historial-fotos-mini`, `status-wa`/`waDeliveryChip`). **Hallazgo aparte:** SCRUM-1057 (fusionar duplicados) esta Finalizada en Jira pero su propio doc dice «la pantalla es de S2» y `customersView.js` servido hoy no contiene `fusion-preview` ni `/fusionar` — no hay UI, comentado en el ticket | `Invoke-WebRequest https://yaqu.app/version` · `Invoke-WebRequest https://yaqu.app/dashboard/js/customerDetailView.js` (contiene `c360-chips`, `historial-fotos-mini`) · `.../customersView.js` (contiene `contacto-fila`, NO contiene `fusion-preview` ni `/fusionar`) · `.../quotesListView.js` (contiene `status-wa`, `waDeliveryChip`) · `git merge-base --is-ancestor <sha-commit> cab692d5` para cada ticket (medido 25-sep-2026 16:38Z, hora de GitHub, sobre `origin/main`) |

## Lo que estas nueve tienen en común

Ninguna es una mentira: **todas empiezan en una observación real**. Lo que falla es el paso
siguiente — convertirla en diagnóstico y escribirla como hecho sin el comando que la sostenga.

Cinco de las nueve se habrían cazado en dos minutos con un comando de una línea. Ése es el coste
de la regla, y por eso la regla es barata.

> 🔒 Contar texto no es contar cosas · una caja CSS no es lo que ocupa · nombrar algo no es
> imprimirlo · un rojo en tu árbol no es un rojo en main.
