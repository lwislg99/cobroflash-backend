{
  "name": "yaqu-backend",
  "version": "1.0.0",
  "private": true,
  "license": "ISC",
  "type": "commonjs",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && node --test --test-force-exit tests/*.test.mjs",
    "//test:staging": "SCRUM-166 (28-jul-2026): test:staging ES la tanda gateada completa (identica a test:staging:gated). Antes era ungated y los 51 gateados salian en SKIP silencioso -- por eso se perdio una tanda el 27-jul. La rapida ungated sigue siendo: npm test. Se mantienen los DOS nombres apuntando al runner porque ~8 documentos citan uno u otro y borrar uno romperia la mitad de las referencias.",
    "test:staging": "npm run build && node scripts/test-staging-gated.mjs",
    "test:staging:gated": "npm run build && node scripts/test-staging-gated.mjs",
    "//turno": "SCRUM-232: el turno de staging, SUELTO. Hasta aqui solo lo tomaba el runner gateado al arrancar, asi que sostenerlo durante otra operacion contra staging obligaba a improvisar un script cada vez. `turno:estado` es solo lectura y es el que evita la conducta peligrosa: quien llega puede saber QUE esta corriendo y si le compensa esperar, en vez de elegir entre esperar a ciegas o romper el lock. Ninguno rompe locks ajenos: para eso esta marcar-staging.mjs.",
    "turno:estado": "node scripts/turno-staging.mjs estado",
    "turno:tomar": "node scripts/turno-staging.mjs tomar",
    "turno:soltar": "node scripts/turno-staging.mjs soltar",
    "prisma:generate": "prisma generate",
    "//postinstall": "SCRUM-238: Railway construye con `npm install` y NADA del repo generaba el cliente de Prisma -- dependia de la auto-deteccion de nixpacks, una dependencia invisible y load-bearing (si nixpacks dejara de auto-generar, prod no arrancaria). Este postinstall lo hace EXPLICITO: aditivo e idempotente. SCRUM-351: aqui se AFIRMABA que en local node_modules se comparte por junction entre worktrees, y de esa frase salio la regla que aplicaban todas las sesiones -- 'quien regenera regenera para todos', luego no se regenera. Medido el 11-ago-2026 con realpath sobre los cuatro worktrees vivos: los cuatro lo tienen PROPIO, la regla no aplica. Y no se cambia una afirmacion por otra, porque la respuesta caduca en cuanto alguien recree un worktree o cambie de maquina: se COMPRUEBA con `npm run topologia`. Lo que si sigue en pie es el guard _prisma-client-guard.mjs (pretest), que caza el cliente desparejado venga de donde venga.",
    "postinstall": "prisma generate",
    "db:push": "bash scripts/db-push-prod",
    "prisma:studio": "prisma studio",
    "db:seed": "prisma db seed",
    "e2e:critico": "npm run build && node scripts/e2e-critico.mjs",
    "//guard:contraste": "SCRUM-368: mide el contraste WCAG AA en NAVEGADOR (Edge via puppeteer-core), no sobre el CSS. Un censo estatico da falsos: no sabe que .sidebar-logo-text vive en el sidebar oscuro ni compone los rgba(). Fuera de `npm test` porque necesita navegador; la red que SI corre siempre es tests/scrum368-contraste-tokens.test.mjs, que vigila los pares de tokens.",
    "guard:contraste": "node scripts/guard-contraste.mjs",
    "//guards:entrada": "SCRUM-414: los cuatro guards que vigilan una entrada de docs/master/ (nombre del fichero 273, ancla de medicion 267, tests declarados 391, documentos nombrados 242). Cada sesion los descubria EN ROJO despues de empujar porque no habia forma de comprobarlos de golpe: `npm test` compila y corre 2.400 tests, asi que nadie lo lanza para revisar un fichero de texto. Estos cuatro son estructurales -sin build y sin BD-, tardan segundos. Su mensaje de fallo nombra este comando, para que la quinta sesion no vuelva a enterarse por el PR.",
    "guards:entrada": "node scripts/guards-entrada.mjs",
    "guard:prisma": "node scripts/_prisma-client-guard.mjs && node scripts/_prisma-procedencia-guard.mjs",
    "//topologia": "SCRUM-351: contesta AHORA si los worktrees comparten node_modules -- propio, enlazado (junction/symlink) o resuelto HACIA ARRIBA, que es el que no deja huella-- en vez de citar una frase escrita hace meses. Fuera de `npm test` porque es una pregunta que se hace, no un gate; el test que lo vigila si corre en la tanda. Sale 1 SOLO cuando no ha podido mirar: compartir es una configuracion legitima, no un fallo, y un fallo de lectura jamas se cuenta como 'son independientes'.",
    "topologia": "node scripts/topologia-node-modules.mjs",
    "//pretest": "SCRUM-429: `_prisma-sync` va DELANTE del guard, no en su lugar. Regenera el cliente cuando el schema de esta rama no cuadra -causa (A), que ningun aislamiento arregla porque el schema viaja con la rama y el cliente no- y SOLO si node_modules es propio: con junction, regenerar arregla el tuyo y rompe el de quien este corriendo tests. El guard sigue detras a proposito: el dia que el automatismo se deshaga queremos enterarnos por el, no por una medicion corrompida.",
    "pretest": "node scripts/_prisma-sync.mjs && node scripts/_prisma-client-guard.mjs && node scripts/_prisma-procedencia-guard.mjs",
    "//pretest:staging": "SCRUM-235: `pretest` SOLO se dispara antes del script `test`. La tanda gateada es OTRO script, asi que la unica tanda que toca una base real era la unica que no comprobaba el cliente: el 29-jul murio en la fase 4 tras 27 minutos y con el turno de staging tomado. Los dos nombres del runner (SCRUM-166) necesitan su propio pre-: npm no los deriva.",
    "pretest:staging": "node scripts/_prisma-client-guard.mjs && node scripts/_prisma-procedencia-guard.mjs",
    "pretest:staging:gated": "node scripts/_prisma-client-guard.mjs && node scripts/_prisma-procedencia-guard.mjs",
    "//puerta:cliente-real": "SCRUM-390: avisa de que entro el primer cliente real (dos senales). Necesita DATABASE_URL; por eso es comando y no test.",
    "puerta:cliente-real": "node scripts/puerta-cliente-real.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.98.0",
    "@prisma/client": "^6.18.0",
    "archiver": "^8.0.0",
    "axios": "^1.12.2",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "node-cron": "^4.2.1",
    "node-fetch": "^3.3.2",
    "nodemailer": "^7.0.10",
    "pdfkit": "^0.17.2",
    "qrcode": "^1.5.4",
    "stripe": "^19.3.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/archiver": "^8.0.0",
    "@types/express": "^5.0.3",
    "@types/node": "^24.5.2",
    "@types/node-cron": "^3.0.11",
    "@types/nodemailer": "^7.0.3",
    "@types/pdfkit": "^0.17.3",
    "@types/qrcode": "^1.5.6",
    "fake-indexeddb": "6.2.5",
    "prisma": "^6.18.0",
    "puppeteer-core": "^25.3.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2",
    "xmllint-wasm": "^5.2.0"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  }
}
